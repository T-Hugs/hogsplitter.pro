---
import "../styles/global.css";

interface Props {
  title: string;
  description: string;
  /** Path under site root, e.g. `/images/prototype.jpg` */
  ogImage?: string;
}

const { title, description, ogImage } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site ?? "https://hogsplitter.pro");
const ogImageUrl = ogImage ? new URL(ogImage, Astro.site ?? "https://hogsplitter.pro") : null;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    {ogImageUrl ? (
      <>
        <meta property="og:image" content={ogImageUrl.href} />
        <meta name="twitter:card" content="summary_large_image" />
      </>
    ) : (
      <meta name="twitter:card" content="summary" />
    )}
    <link rel="icon" href="/images/logo-640.png" type="image/png" sizes="any" />
    <link rel="apple-touch-icon" href="/images/logo-640.png" />
    <title>{title}</title>
  </head>
  <body>
    <div class="site-shell">
      <header class="site-header">
        <a class="brand-mark" href="/" aria-label="Hog Splitter Pro home">
          <img
            class="brand-mark__logo"
            src="/images/logo-640.png"
            alt=""
            decoding="async"
          />
        </a>
        <nav class="nav-links" aria-label="Primary navigation">
          <a href="/">Home</a>
          <a href="/guide/">Guide</a>
          <a href="/#photos">Photos</a>
          <a href="/#contact">Contact</a>
          <a class="button-secondary" href="https://www.etsy.com/shop/HugsCurlingTech">Shop on Etsy</a>
        </nav>
      </header>
      <main>
        <slot />
      </main>
      <footer class="site-footer">
        <div class="footer-inner">
          <span>© 2026 Hugs Platforms.</span>
          <span>Hog Splitter Pro · Purposefully designed curling timing</span>
        </div>
      </footer>
    </div>
  </body>
</html>
